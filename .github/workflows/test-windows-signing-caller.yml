name: "Test Windows Signing Caller"

on:
  push:
    branches:
      - aaif-signing-windows
  workflow_dispatch:

permissions:
  id-token: write
  contents: read

jobs:
  call-signing:
    uses: ./.github/workflows/test-windows-signing.yml
    with:
      signing: true
    secrets: inherit
